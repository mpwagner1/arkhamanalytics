from setuptools import setup, find_packages

setup(
    name='arkhamanalytics',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    description='Reusable ingestion framework for Databricks',
)
